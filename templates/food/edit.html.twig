{% extends 'base.html.twig' %}

{% block title %}Edit Food{% endblock %}

{% block body %}
    <h1>Edit Food</h1>
    <div class="row">
        <div class="col-3">
            <div class="mb-3">{{ include('food/_form.html.twig', {'button_label': 'Update'}) }}</div>
            <div><a href="{{ path('app_food_index') }}">back to list</a></div>
        </div>
    </div>
{% endblock %}
