{{ form_start(form) }}
<div class="row">
    <div class="col-6">
        {{ form_row(form.meal_type) }}
    </div>
</div>
<div class="row">
    <div class="col-6">
        {{ form_row(form.date) }}
    </div>
</div>        
{{ form_end(form) }}
