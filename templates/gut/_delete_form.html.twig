<form method="post" action="{{ path('app_gut_delete', {'id': gut.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gut.id) }}">
    <button class="btn">Delete</button>
</form>
