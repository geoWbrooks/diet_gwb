{% extends 'base.html.twig' %}

{% block title %}Gut index{% endblock %}

{% block body %}
{#    {{ dump(chart) }}#}
{#    {{ dump(render_chart(chart)) }}#}
    {{ render_chart(chart) }}
{% endblock %}
